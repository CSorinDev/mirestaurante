<form method="post" action="{{ path('app_admin_carta_delete', {'id': carta.id}) }}" onsubmit="return confirm('¿Estás seguro de eliminar este elemento?');" style="display:inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carta.id) }}">
    <button class='emoji'>❌</button>
</form>