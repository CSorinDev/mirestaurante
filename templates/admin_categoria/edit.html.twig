{% extends 'base.html.twig' %}

{% block title %}Edit Categoria
{% endblock %}

{% block body %}
	<main>
		<h1>Editar Categoria</h1>

		{{ include('admin_categoria/_form.html.twig', {'button_label': 'Actualizar'}) }}

		<div class="flex-center">
			{{ include('admin_categoria/_delete_form.html.twig') }}
			<a href="{{ path('app_admin_categoria_index') }}">Volver a la lista</a>
		</div>
	</main>
{% endblock %}
