{% extends 'base.html.twig' %}

{% block title %} - Admin Categoría{% endblock %}

{% block body %}
	<main>
		<h1>Categorías</h1>

		<a class="btn center-link" href="{{ path('app_admin_categoria_new') }}">Añadir Categoría</a>

		{% if categorias %}
			<table>
				<thead>
					<tr>
						<th>Nombre</th>
						<th>Orden</th>
						<th>Acciones</th>
					</tr>
				</thead>
				<tbody>
					{% for categoria in categorias %}
						<tr>
							<td>{{ categoria.nombre }}</td>
							<td>{{ categoria.orden }}</td>
							<td>
								<a class='emoji' href="{{ path('app_admin_categoria_show', {'id': categoria.id}) }}">🔍</a>
								<a class='emoji' href="{{ path('app_admin_categoria_edit', {'id': categoria.id}) }}">✏️</a>
								<a class='emoji' href="{{ path('app_admin_categoria_delete', {'id': categoria.id}) }}">❌</a>
							</td>
						</tr>
					{% endfor %}
				</tbody>
			</table>
		{% else %}
			<br>
			<p>No hay categorias disponibles</p>
		{% endif %}
	</main>
{% endblock %}
