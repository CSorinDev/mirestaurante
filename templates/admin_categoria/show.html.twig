{% extends 'base.html.twig' %}

{% block title %}
	- Ver Categoría
{% endblock %}

{% block body %}
	<main>
		<h1>Categoria</h1>

		<table class="table">
			<thead>
				<th>Nombre</th>
				<th>Orden</th>
			</thead>
			<tbody>
				<tr>
					<td>{{ categoria.nombre }}</td>
					<td>{{ categoria.orden }}</td>
				</tr>
			</tbody>
		</table>

		<div class="flex-center">
			<a class="" href="{{ path('app_admin_categoria_index') }}">Volver a la lista</a>
			<a class="btn" href="{{ path('app_admin_categoria_edit', {'id': categoria.id}) }}">
				Editar
			</a>
			{{ include('admin_categoria/_delete_form.html.twig') }}
		</div>
	</main>
{% endblock %}
