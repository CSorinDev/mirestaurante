{% extends 'base.html.twig' %}

{% block body %}
	<main>
		<h1>Mis Reservas</h1>

		{% for message in app.flashes('success') %}
			<p class="alert alert-success">
				{{ message }}
			</p>
		{% endfor %}

		{% if reservas is empty %}
			<section class="reservas-list-no-reservas">
				<p>No tienes ninguna reserva todavía.</p>
				<a class="btn" href="{{ path('app_reserva') }}" class="btn btn-primary">
					Hacer una Reserva
				</a>
			</section>
		{% else %}
			<section>
				<table class="center-table">
					<tr>
						<th>Fecha</th>
						<th>Día</th>
						<th>Hora</th>
						<th>Comensales</th>
						<th>Cancelar</th>
					</tr>
					{% for reserva in reservas %}
						<tr>
							<td>
								{{ reserva.fecha | date('d/m/Y') }}
							</td>
							<td>
								{{ reserva.fecha | format_datetime(pattern="EEEE", locale="es") }}
							</td>
							<td>
								{{ reserva.hora | date('H:i') }}
							</td>
							<td>
								{{ reserva.comensales }}
							</td>
							<td>
								{{ include('/reserva/_delete_form.html.twig') }}
							</td>
						</tr>
					{% endfor %}
				</table>
			</section>
		{% endif %}
	</div>
</main>{% endblock %}
